

      【評価　<strong><%= question.question_scores.count %></strong>　件】　

  <% if QuestionScore.find_by(user_id: current_user.id, question_id: question.id) %>

      <%= link_to "取り消す",question_question_score_path(question), method: :delete, remote: true %>

  <% else %>

      <%= link_to "評価",question_question_score_path(question), method: :post, remote: true %>

  <% end %>
